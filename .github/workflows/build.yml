on:
  workflow_dispatch:

jobs:
  install:
    name: Run ROCm installer
    runs-on: ubuntu-latest
    steps:
      - use: actions/checkout@v4
      - uses: ./
      - run: |
          which hipcc
          ls -lhat /opt/
          ls -lhat /opt/rocm/
    
